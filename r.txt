pip install -r requirements.txt
波形图无法缩放和滚动、无法交互（用户无法在波形图上通过鼠标拖拽边缘来微调歌词的起止点）对精调时间轴毫无实质性帮助，请增加对应功能
Ctrl+Space作为播放/暂停的全局快捷键，避免和打字的时候冲突。Space在不聚焦于打字的时候也可以作为播放/暂停的快捷键使用。
打拍模式，当用户随着歌声按空格为每个字赋时间时，前一个字的结束时间等于当前字的开始时间。但是，当敲完最后一个字时，打拍流程就结束了，软件无法感知最后一个字在哪里结束。代码中直接将最后一个字符的结束时间粗暴地指定为整行的结束时间，这不合理，请修改完善
行打拍モード缺乏“容错撤销”机制，应当可以撤销
支持LRC 导入导出，可以导入标准 LRC 作为底稿，或者把当前的歌词导出为标准 LRC 文件，方便在其他软件中使用。